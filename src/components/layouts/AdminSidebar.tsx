// Novo arquivo para sidebar consistente do Admin
'use client';

import React from 'react';
import { adminNavItems } from '@/lib/navigation';

// Exportar o array de itens do sidebar para ser usado em toda a aplicação
export { adminNavItems };

// Este arquivo serve como um local centralizado para definir os itens do sidebar do admin
// Isso garante consistência em todas as páginas do admin 